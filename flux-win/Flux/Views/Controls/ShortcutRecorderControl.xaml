<UserControl x:Class="Flux.Views.Controls.ShortcutRecorderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="#10FFFFFF" CornerRadius="6" Padding="6,3" MinWidth="120"
            BorderBrush="#18FFFFFF" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="DisplayText" Grid.Column="0"
                       Foreground="#CCFFFFFF" VerticalAlignment="Center"
                       Text="Space" Margin="4,0" FontSize="12"/>
            <Button x:Name="RecordButton" Grid.Column="1"
                    Content="Record" Width="56" Height="22"
                    Click="OnRecordClick"
                    FontSize="11" Padding="6,2"/>
        </Grid>
    </Border>
</UserControl>
